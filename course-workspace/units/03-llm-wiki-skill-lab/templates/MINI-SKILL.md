---
name: compile-raw-to-wiki
description: 将 1–5 篇由 Web Clipper 保存的 Raw 按顺序编译进已有个人 Obsidian Wiki，并维护来源、索引和日志。仅在人明确调用并提供 Raw 列表时使用；不负责第一次搭建 Wiki。
---

# compile-raw-to-wiki

本 Skill 的 Codex 调用策略写在 `agents/openai.yaml`，默认只接受人的显式调用。

## 什么时候使用

个人 `Inbox/Raw/` 中已有 1–5 篇正文和来源可读的文章，并希望把它们整合进已经存在的 `Learn/Wiki/` 时使用。

## 什么时候不用

- 只有网页链接，还没有本地 Raw。
- Raw 正文为空、来源缺失或 Web Clipper 前置未验证。
- 个人 Obsidian Vault、现有 Wiki 根目录、规则说明、Index 或 Log 不明确。
- 目标只是即时问答，不需要长期知识文件。
- 任务要求第一次搭建 Wiki、修改 Raw、安装其他 Skill 或执行外部动作。

## 输入

- 个人 Obsidian Vault 绝对路径。
- 1–5 篇 Raw 的有序绝对路径和对应原文链接。
- 现有 Wiki 根目录 `<Vault>/Learn/Wiki/`。
- 本次最想保留或检查的问题，可选。

## 稳定规则

- Raw 只读，不修改、不移动、不重命名、不删除。
- 只使用现有 Wiki；先读规则说明、Index、Log 和相关页面，不建立平行 Wiki。
- 按输入顺序逐篇处理，不并行写入。
- 每篇 Raw 都必须有可回查的来源记录。
- 优先更新已有 Concept、Entity、Comparison 或来源知识，不制造近义重复页面。
- 可选页面只在有长期复用价值时创建；页面数量不是完成标准。
- 每篇完成后都记录创建、更新、复用、未创建及原因。
- 写完后重读变化，等待人检查；不替人宣布内容值得保留。

## 步骤

1. 验证现有 Wiki 的规则说明、Index、Log 和根目录；缺少任一项就停止。
2. 按顺序验证每篇 Raw 的路径、正文、来源和抓取时间，记录处理前状态。
3. 对当前 Raw 读取相关 Wiki 页面，判断新建还是更新，并说明理由。
4. 创建或更新必要知识，同时维护 Index 和 Log。
5. 重读当前 Raw 对应的所有变化，检查来源、链接、重复和不确定点。
6. 确认当前 Raw 没有变化，记录本篇状态后再进入下一篇。
7. 全部处理后重读整批变化，报告完成项、阻断项、创建、更新和复用文件。
8. 等人抽查 Raw、Index、Log 和知识内容；根据反馈只做必要修正并写入 Log。

## 停止条件

- 任一 Raw 缺失、为空或来源不可回查。
- 个人 Vault 或现有 Wiki 根目录不明确。
- 规则说明、Index 或 Log 缺失，需要第一次搭建 Wiki。
- 发现两套冲突规则，无法判断应沿用哪一套。
- 操作需要修改 Raw、删除文件、写出个人 Vault、联网发布或执行其他外部动作。

发生停止条件时，保留已完成记录，明确指出哪一篇被阻断及原因，不把整批报告为完成。

## 输出

输出不是固定数量的页面，而是对同一套 Wiki 的可解释更新。最终逐篇报告：来源、创建文件、更新文件、复用页面、未创建的可选页面、未知点和 Raw 前后检查。

## 完成标准

只有以下条件全部满足，才能报告整批完成：

- 输入列表中的每篇 Raw 都标记为完成，没有未说明的跳过或阻断。
- 所有 Raw 保持不变，每篇内容都能回查来源。
- 同一套 Wiki 的 Index 和 Log 已更新，没有建立平行结构。
- 已有知识被优先复用或更新，新增页面有明确理由。
- 人完成约定的文件抽查，必要修正已写入 Log。
